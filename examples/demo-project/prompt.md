# Demo Mission

This is a placeholder mission prompt used by the example project definition
(`config/projects/example.json`). Replace it with your real mission.

## Your task

Create a file called `hello.txt` in this directory containing the text
"Hello from a supervised AI session". Then verify the file exists and read
it back.

## Completion

When — and only when — every task above is finished and verified, output the
exact text:

MISSION COMPLETE
